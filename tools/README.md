# tools/ — historical HUF tooling

HUF-era pipeline + diagnostics. The **current** deterministic toolchain is in the Hs repo (`HCI-CNTT/run_cntt.py` + `tools/cntt_report.py`). These are preserved as the development-history tools. See `../RELATIONSHIP_TO_Hs.md`.
